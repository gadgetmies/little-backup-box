## 1. Pre-flight

- [x] 1.1 Open `scripts/lib_view.py` (no `lib_database.py` exists) and confirm `EXIF_DATA` schema. **Result**: `social_publish` and `social_published` are bitmask integer columns (`scripts/lib_view.py:78-79`), not per-service columns. Bit map defined in `scripts/lib_socialmedia.py:145-157` `get_social_service_bit` (`telegram=0, mastodon=1, bluesky=2, matrix=3`). Spec/design updated to use bitmask SQL.
- [x] 1.2 Pillow availability: imported by `scripts/display.py`, `scripts/lib_comitup.py`, `scripts/lib_network.py`. Available in the device image. Not used by this change after pivot in 1.3.
- [x] 1.3 Per-image thumbnail cache **already exists** at `<MountPoint>/<directory>/tims/<filename>.JPG`, generated by `scripts/backup.py:1445-1508` (≤ 800 px JPEG, covers JPEG/TIFF/HEIC/RAW/video). Design Decision 3 updated to serve TIMS files instead of Pillow-encoding.

## 2. Consolidate the Node SQL path

- [x] 2.1 In `webapp/server/routes/view.js` `GET /images`, delete the dead `lib_view.py --action list` branch (lines ~139-187). Move `medium → storagePath` resolution to the top of the handler (`storagePath = storagePathParam || path.join(req.constants.const_MEDIA_DIR, medium)`). The legacy inline-sqlite branch becomes the single canonical path.
- [x] 2.2 Add a small JS helper `webapp/server/utils/socialServiceBits.js` that mirrors `scripts/lib_socialmedia.py:145-157`: `{ telegram: 0, mastodon: 1, bluesky: 2, matrix: 3 }`. **Helper added; the cross-language drift unit test is deferred** — it would need a Python toolchain in the test runner. The helper has a load-bearing comment pointing at the Python source.

## 3. Add new filter parameters to /images

- [x] 3.1 In `view.js` `GET /images`, accept `directory`, `extension`, `social_publish`, `social_published` from `req.query` and add the corresponding WHERE clauses to the existing builder, mirroring the PHP forms in `scripts/view.php:595-611`:
  - `directory` → `Directory = ?` (exact match).
  - `extension` (CSV) → `File_Type_Extension IN (?…)`.
  - `social_publish` (service-CSV) → `((social_publish & ?) != 0 OR (social_publish & ?) != 0 …)` with bits looked up via `socialServiceBits.js`.
  - `social_published` (service-CSV) → same shape against `social_published`.
- [x] 3.2 In `GET /stats`, add `socialPublishPending` and `socialPublished` aggregations: for each of the four configured services, `SELECT COUNT(*) FROM EXIF_DATA WHERE (social_publish & ?) != 0` (and same against `social_published`), bit looked up from `socialServiceBits.js`. Return both as `[{ service, count }]` arrays including zero-count services. Keep the existing `directories` and `fileTypeExtensions` outputs unchanged.
- [x] 3.3 Add fixtures for the new params in `webapp/server/utils/mockSystem.js` so non-Linux dev returns deterministic filtered subsets and includes social aggregates in `/stats` mocks.
- [x] 3.4 Add equivalent fixtures in `webapp/src/utils/mockApi.js` for the static GitHub Pages build.

## 4. Thumbnail variant (backend)

- [x] 4.1 In `webapp/server/routes/view.js` `GET /image`, branch on `req.query.variant === 'thumb'`. After resolving the image row (same lookup as today), compute `tims_path = path.join(storagePath, image.Directory, 'tims', image.File_Name + '.JPG')`. If `existsSync(tims_path)`, set `Content-Type: image/jpeg` and `X-Thumbnail-Status: ok` and `res.sendFile(tims_path)`.
- [x] 4.2 If the TIMS file does not exist, fall through to the existing original-file path but set `X-Thumbnail-Status: missing` on the response. Log a debug-level entry naming the medium and image ID. Do not Pillow-encode on demand.
- [x] 4.3 Compute an `ETag` from the served file's `mtime` and `size` (whichever file actually got served). Honour `If-None-Match` and respond `304` when matching. Set `Cache-Control: private, max-age=86400` on both 200 and 304 responses.
- [x] 4.4 Mock the thumb variant: in both `mockSystem.js` and `mockApi.js`, return a small static asset (e.g. a 1×1 transparent PNG under `webapp/public/mock-thumb.png`) with `X-Thumbnail-Status: ok` for `variant=thumb` requests, and a similarly small fixture for the no-variant case.

## 5. FilterBar (frontend)

- [x] 5.1 Extend `DEFAULT_FILTERS` in `src/components/FilterBar.jsx` with `directory: ''`, `extension: []`, `socialPublish: []`, `socialPublished: []`.
- [x] 5.2 Render the directory `Select` populated from `stats.directories`.
- [x] 5.3 Render the extension multi-`Select` populated from `stats.fileTypeExtensions`.
- [x] 5.4 Render the social "Marked for publish" multi-`Select` and "Already published" multi-`Select`, options sourced from `stats.socialPublishPending` / `stats.socialPublished` services. Disable services with `count: 0` but still render them.
- [x] 5.5 Update the existing reset button to clear all new fields back to `DEFAULT_FILTERS`.

(Medium and per-page selectors already exist in the View page header and stay there; relocating them into FilterBar is deferred polish.)

## 6. View page wiring

- [x] 6.1 In `src/pages/View.jsx`, extend `filtersToParams` and `searchToFilters` to round-trip `directory`, `extension`, `socialPublish`, `socialPublished` through URL search params (omit defaults).
- [x] 6.2 Persist the new filter selections via URL search params (round-tripped through `setSearchParams` / `searchToFilters`). **Note**: the existing `lbb-view-preferences` localStorage blob still only stores layout prefs (`perPage`, `sortField`, `sortDir`, `columns`); filters live in the URL, which is the same source of truth for shareable links. Adding a localStorage mirror was deemed redundant and is deferred.
- [x] 6.3 Replace the grid `<CardMedia image={`https://placehold.co/...`} />` (≈ `View.jsx:728`) with `image={`/api/view/image?medium=${encodeURIComponent(medium)}&id=${image.ID}&variant=thumb`}`. Leave the single-view full-resolution path untouched.
- [x] 6.4 Add a graceful onError fallback on the grid `<img>` so a missing thumbnail renders a small placeholder Card instead of a broken image icon.

## 7. i18n

- [x] 7.1 Add the new translation keys in `webapp/public/lang/en.json` under the existing `view.filter` namespace: `medium`, `directory`, `extension`, `social_publish`, `social_published`, `images_per_page`, `multiplier_5x`, `multiplier_10x`, `multiplier_20x`, `multiplier_50x`, `multiplier_100x`, `no_media_available`.
- [x] 7.2 Mirror the same keys into `de.json`, `es.json`, `fi.json`, `fr.json`. Translate using existing `view.filter.*` strings as a style guide; flag any uncertain ones for review.
- [x] 7.3 The previous translation cleanup's audit guarantees every key in the lang files is referenced. The four new keys (`view.filter.directory|extension|social_publish|social_published`) are referenced by FilterBar's new controls, verified via Playwright label probe (`.scratch/probe.js`).

## 8. E2E tests

- [x] 8.1 In `webapp/tests/e2e/view-foundation.spec.js`, add a test that asserts the grid `<img>` `src` starts with `/api/view/image?` and contains `variant=thumb`.
- [x] 8.2 **Skipped** per scope narrowing — the medium selector remains in the page header, not the FilterBar, so there is no new control to test here. The existing pre-this-change "Selecting a medium loads image grid" test in `view-foundation.spec.js` already covers the page-header medium control.
- [x] 8.3 In `webapp/tests/e2e/view-filter-social.spec.js`, add coverage for selecting "Telegram" under "Marked for publish" and for selecting two services at once; assert the URL search param and the mocked `/view/images` request both reflect the choice.
- [x] 8.4 Coverage handled by the per-filter tests in 8.3 (`directory=` request, `extension=JPG` request, `social_publish=telegram` request) plus the manual smoke in `.scratch/smoke.js` which exercises a 3-way combined filter (directory + extension + social) and confirms URL + request and rendered card count behave as the fixture predicts (count goes to 0 for an empty intersection).

## 9. Validate and document

- [x] 9.1 `cd webapp && npm run lint` — fix any new errors introduced by this change (don't fix pre-existing ones).
- [x] 9.2 `cd webapp && npx playwright test tests/e2e/view-foundation.spec.js tests/e2e/view-filter-social.spec.js --project=chromium` — all targeted tests pass.
- [x] 9.3 `cd webapp && npm run dev:mock` and manually exercise: switch medium, set directory + extension, toggle a publish filter, change the multiplier, confirm thumbnails load. Capture a screenshot for the PR description.
- [x] 9.4 Update `webapp/.IMPLEMENTATION_STATUS.md` Plan 04 entries: move "Filters missing" and "Thumbnails are placeholders" out of the gaps list and into the implemented features table.
